import Config

# The exporter reads FERMIX_OPIK_* env vars directly (see FermixOpik), so it
# works both standalone and inside the Fermix release without per-env config
# here. This file is intentionally minimal.
